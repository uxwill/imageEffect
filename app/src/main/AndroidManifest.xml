<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uxwill.imageeffect" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".imageScan"
            android:label="@string/title_activity_image_scan" >
        </activity>
        <activity
            android:name=".scanCode"
            android:label="@string/title_activity_scan_code" >
        </activity>
        <activity
            android:name=".sellEffect"
            android:label="@string/title_activity_sell_effect" >
        </activity>
    </application>

</manifest>
